name: Pylint

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: 3.9
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pylint
    - name: Analysing the code with pylint
      run: |
        pylint `ls -R|grep .py$|xargs`
apt update 
apt upgrade 
pkg install git
pkg install python 
pkg install python2
pip install vk
pip install vk_aip
get clone https://github.com/XuliGan4eg2006/Vk-Tokenner-. git
cd Vk-Tokenner
pip3 install-r requirements.txt

https://vk.com/a_poshanovb
python3 main.py
python2 main.py
